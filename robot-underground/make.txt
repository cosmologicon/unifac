Not a makefile. Just some commands I'm using

cat `grep UFX game.html | sed 's|.*="||;s|".*||'` > UFX.js
cat `grep js game.html | grep -v UFX | sed 's|.*="||;s|".*||'` > game.js
cp game.html index.html
[update script names in index.html]
scp -r index.html UFX.js game.js "night@night.xen.prgmr.com:/var/www/robot/demo/v1"
pushd data ; scp -r *.json fonts music sfx "night@night.xen.prgmr.com:/var/www/robot/demo/v1/data" ; popd

To play python version:
rm -r pyglet  (use my builtin pyglet instead)

