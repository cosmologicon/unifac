// Convenient global variable containing settings

// Options like screen resolution (ie, things that the player might reasonably change)
var settings = {
    // Canvas size
    sx: 800,
    sy: 500,
    // Swap left/right in combos when facing left
    swapcontrols: true,
}

// Game mechanics (players shouldn't generally change these)
var mechanics = {
    runvx: 200,  // vx on the ground
    turnvx: 0, turnvy: 200,
}



